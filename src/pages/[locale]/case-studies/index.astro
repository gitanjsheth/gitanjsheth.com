---
import { getCollection } from "astro:content";
import BaseLayout from "../../../layouts/BaseLayout.astro";
import CtaBand from "../../../components/CtaBand.astro";
import {
  localeStaticPaths,
  localizePath,
  useTranslations,
  type Locale,
} from "../../../i18n/utils";

export function getStaticPaths() {
  return localeStaticPaths();
}

const locale = Astro.params.locale as Locale;
const t = useTranslations(locale);

const studies = (await getCollection("caseStudies")).sort(
  (a, b) => a.data.order - b.data.order
);
---

<BaseLayout locale={locale} pagePath="case-studies" title={t("caseStudies.title")} description={t("caseStudies.intro")}>
  <section>
    <div class="container-x pt-10 md:pt-16 pb-8 md:pb-10">
      <h1 class="h1">{t("caseStudies.title")}</h1>
      <p class="mt-5 max-w-3xl text-lg text-muted">{t("caseStudies.intro")}</p>
    </div>
  </section>

  <section class="border-t border-ink/10">
    <div class="container-x pt-8 md:pt-10 pb-10 md:pb-16">
      <div class="grid gap-5 md:grid-cols-2">
        {
          studies.map((c) => (
            <a
              href={localizePath(locale, `case-studies/${c.id}`)}
              class="card group flex h-full flex-col p-7"
            >
              <p class="text-xs uppercase tracking-caps text-muted">
                {c.data.confidential ? t("caseStudies.confidential") : c.data.client}
                <span class="mx-2 text-gold">·</span>
                {c.data.sector[locale]}
                {c.data.placeholder && <span class="ml-2 opacity-60">· placeholder</span>}
              </p>
              <h2 class="mt-3 h3 leading-snug">{c.data.title[locale]}</h2>
              <p class="mt-3 flex-1 text-muted">{c.data.problem[locale]}</p>
              <span class="mt-5 text-sm text-gold">{t("caseStudies.readMore")}</span>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <CtaBand locale={locale} />
</BaseLayout>
